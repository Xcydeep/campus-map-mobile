class HiveHelper {
  /// Minimal initializer for Hive-related setup. Add box registrations here.
  static Future<void> init() async {
    // Example: open a box if needed in the future. Keep no-op for now.
  }
}
