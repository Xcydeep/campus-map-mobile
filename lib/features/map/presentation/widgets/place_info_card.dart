import 'package:flutter/material.dart';

class PlaceInfoCard extends StatelessWidget {
  final dynamic place;
  const PlaceInfoCard({super.key, this.place});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
